## Saint Bernard

1. Install gems:

		bundle install

2. Setup database:

		rake db:setup

3. Run specs with coverage:

		COVERAGE=true rspec

4. Run app:

		rails s

5. Open [http://localhost:3000](http://localhost:3000).
